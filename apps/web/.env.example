# Supabase connection for the web app.
# These are safe to ship in the browser bundle: the publishable key only grants
# access allowed by Row Level Security. NEVER put the service-role key here.
VITE_SUPABASE_URL=https://vpklztxqkvqmmzsxfqgp.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxx

# Copy this file to `.env.local` and fill in real values (see project README).
