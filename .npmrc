# Expo / React Native work most reliably with a hoisted node_modules layout in a
# pnpm monorepo. See https://docs.expo.dev/guides/monorepos/
node-linker=hoisted

# React Native's dependency graph has many loose peer ranges; don't fail install on them.
strict-peer-dependencies=false
